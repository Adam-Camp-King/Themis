name: ci
on:
  push:
    branches: [main]
  pull_request:
jobs:
  typescript:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: npm run typecheck
      # Build before test: the examples import each package's dist/, and the
      # root build compiles themis-policy (core) first so the adapters resolve it.
      - run: npm run build
      - run: npm test
  python:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "${{ matrix.python }}" }
      - run: pip install -e './python[test]'
      - run: pytest -q python/tests
  # Both jobs run spec/conformance/* — the vectors are the contract between the
  # TypeScript and Python implementations. A change that passes one and not the
  # other is a spec violation, not a language quirk.
